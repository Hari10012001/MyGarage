package com.mygarage.model.enums;

/**
 * User roles in MyGarage.
 * NORMAL_USER - regular vehicle owner
 * ADMIN - system administrator (seeded, not self-registerable)
 */
public enum Role {
    NORMAL_USER,
    ADMIN
}
